local toggleterm = require('toggleterm')

toggleterm.setup({
    size = 50,
    open_mapping = [[<A-o>]],
    hyde_numbers = true,
    start_in_insert = true,
    insert_mappings = true,
    close_on_exit = true,
})
